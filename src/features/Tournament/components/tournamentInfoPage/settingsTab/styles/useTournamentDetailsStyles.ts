import { makeStyles, Theme } from "@material-ui/core";

export const useTournamentDetailsStyles = makeStyles((theme: Theme) => ({
  invitationalLinkTextRow: {
    display: "flex",
  },
  copyButton: {
    color: theme.palette.common.white,
  },
}));
