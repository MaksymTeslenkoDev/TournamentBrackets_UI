import { makeStyles, Theme } from "@material-ui/core";

export const useTournamentsDashboardInnerStyles = makeStyles(
  (theme: Theme) => ({
    root: {
      padding: theme.spacing(1, 1, 4, 1),
    },
  })
);
